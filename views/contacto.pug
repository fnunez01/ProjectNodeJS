extends layout

block content

  main.container(role='main')
    .shop
      .shop-img
          img(src="./img/banner-contacto.png", alt="") 
      .shop-body
        h1.titulo 
    
    p.lead
      | Contactanos por medio del siguiente Formulario:

    .container

    .row
      .col-md-12
        form(action='contacto' method="POST")
         .form-group
          label(for='nya')
           strong Nombres y Apellidos
          input#nya.form-control(type='text' name="txtNombres" aria-describedby='nyaHelp'  placeholder='Ingresa tus Nombres y Apellidos', required)
          small#nyaHelp.form-text.text-muted Por favor, Nombres y Apellidos Completos.

         .form-group
          label(for='email')
           strong Email
          input#email.form-control(type='email' name="txtEmail" aria-describedby='emailHelp' placeholder='Ingresa tu Email', required)
          small#emailHelp.form-text.text-muted Tu correo activo.

          .form-group
          label(for='mensaje')
           strong Mensaje
          textarea#comment.form-control(rows='5', name="txtMensaje" required)
          small#mensajeHelp.form-text.text-muted Tu Mensaje.
          br  
          button.btn.btn-primary(type='submit') Enviar


    hr

  footer
  .container
    div(align='center')
      | 
      a Web de trabajo

